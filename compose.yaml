services:
  rabbitMQ:
    image: rabbitmq:4.0
    restart: always
    env_file:
      - rabbit.env
    ports:
      - 5672:5672
    healthcheck:
      test: ["CMD", "rabbitmqctl", "status"]
      interval: 30s
      timeout: 10s
      retries: 5